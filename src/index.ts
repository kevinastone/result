/* eslint @typescript-eslint/generic-type-naming: off */

import {Result, resultify} from './result';
import {Option, join, optionify} from './option';

export type {Result};
export {resultify};

export type {Option};
export {join, optionify};
